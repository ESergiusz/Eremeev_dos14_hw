#!/bin/bash
#‘ункци€ по проверке корректности введенного Url
url (){
  regex="^((https?)\:\/\/)?([a-z0-9]{1})((\.[a-z0-9-])|([a-z0-9-]))*\.([a-z]{2,6})(\/?)$"
   read -p "¬ведите url: " address
     if [[ "$address" =~ $regex ]]
   then echo "¬веденый url соответствует стандартам формата дл€ адреса интернет странички"
  else
   echo "¬веденый url не соответствует стандартам формата дл€ адреса интернет странички"
   fi
   }