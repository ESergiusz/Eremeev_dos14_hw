#!/bin/bash
#‘ункци€ по проверке корректности введенного email
email (){
  regex="^([a-z0-9_-]+\.)*[a-z0-9_-]+@[a-z0-9_-]+(\.[a-z0-9_-]+)*\.[a-z]{2,6}$"
   read -p "¬ведите email: " mail
     if [[ "$mail" =~ $regex ]]
   then echo "¬веденый email соответствует стандартам формата дл€ адреса электронной почты"
  else
   echo "¬веденый email не соответствует стандартам формата дл€ адреса электронной почты"
   fi
   }