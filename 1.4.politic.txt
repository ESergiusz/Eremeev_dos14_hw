set -x
#‘ункци€ по проверке корректности введенного password
politic (){
  regex="^(?=.*[0-9].*)(?=.*[a-z].*)(?=.*[A-Z].*)[0-9a-zA-Z$%_#]{8,}$"
   read -p "¬ведите password: " password
     if [[ "$password" =~ $regex ]]
   then echo "¬веденый password соответствует стандартам безопасности дл€ паролей"
  else
   echo "¬веденый password не соответствует стандартам безопасности дл€ паролей"
   fi
   }